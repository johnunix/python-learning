t = int(input())
for i in range(t):
    n, s = map(int, input().split())
    x = list(map(int, input().split()))

    x_min = x[0]
    x_max = x[-1]


    steps = (x_max - x_min) + min(abs(s -x_min), abs(s- x_max))
    print(steps)
    
